<?xml version="1.0" encoding="UTF-8"?>

<rss version="2.0"
  xmlns:content="http://purl.org/rss/1.0/modules/content/"
  xmlns:dc="http://purl.org/dc/elements/1.1/"
  xmlns:media="http://search.yahoo.com/mrss/"
  xmlns:atom="http://www.w3.org/2005/Atom"
  xmlns:georss="http://www.georss.org/georss">

  <channel>
    <title>
      <![CDATA[  Justin Ochalek  ]]>
    </title>
    <link> https://www.justinochalek.com/ </link>
    <description>
      <![CDATA[  Justin&#39;s personal website  ]]>
    </description>
    <atom:link
      href="https://www.justinochalek.com/feed.xml"
      rel="self"
      type="application/rss+xml" />


<item>
  <title>
    <![CDATA[  Julia on Kaggle  ]]>
  </title>
  <link> https://www.justinochalek.com/blog/post2/index.html </link>
  <guid> https://www.justinochalek.com/blog/post2/index.html </guid>
  <description>
    <![CDATA[  Competing on Kaggle with Julia.  ]]>
  </description>  
  
  <content:encoded>
    <![CDATA[  <h1 id="julia_on_kaggle">Julia on Kaggle</h1>
<div class="franklin-toc"><ol><li>Setup</li><li>Docker</li><li>Kaggle Private Datasets</li><li>Run the Notebook</li><li>Final thoughts</li><li>TLDR;</li></ol></div>
<h1 id="background">Background</h1>
<p>Julia is not on Kaggle. </p>
<p>There are <a href="https://www.kaggle.com/code/marketneutral/julia-live-on-kaggle">a few ways</a> to get Julia into a Jupyter notebook on the cloud. The tricky part is using Julia for a Code Competition where submissions run without internet access. This problem is half solved by <a href="https://www.kaggle.com/docs/datasets#creating-a-dataset">private Kaggle Datasets</a>. But how to organize your code as a Kaggle Dataset?</p>
<p>At first I turned to <a href="https://julialang.github.io/PackageCompiler.jl/dev/apps.html">PackageCompiler.jl</a> and conveniently packed my whole inference pipeline into a standalone App that I could execute in a Kaggle Notebook environment. An added bonus was the ability to precompile every function in the pipeline significantly speeding up submission. One downside was the App became bloated with Julia Artifacts from project dependencies even if the inference pipeline did not use them. It seemed possible to cut down on the App size by being more explicit about what PackageCompiler.jl needs, but not without significant effort on my part when I would rather be iterating over my Kaggle submission. Ultimately my App broke after I transitioned from the latest <a href="https://github.com/FluxML/FastAI.jl">FastAI.jl</a> release to the &rsquo;master&rsquo; branch to access some of the upcoming features. I will likely try this again with my next Kaggle competition and a hopefully more stable pipeline.</p>
<p>As a last resort I turned to packaging my pipeline into a handful of tarballs to unpack in the Kaggle Notebook environment and hoped for the best.</p>
<h1 id="step_by_step">Step By Step</h1>
<h2 id="setup">Setup</h2>
<p>I started by downloading the competition dataset using the Kaggle API, training a model, and developing an inference &amp; submission pipeline locally on my desktop. My inference pipeline was organized as a Julia package, for example:</p>
<pre><code class="language-julia">julia&gt; using Pkg
julia&gt; Pkg.generate&#40;&quot;Inference&quot;&#41;</code></pre>
<p>I developed my &ldquo;Inference&rdquo; package so that it could be called from the command line to produce a submission in the correct format.</p>
<pre><code class="language-julia">julia --project&#61;Inference/ Inference/src/Inference.jl</code></pre>
<h2 id="docker">Docker</h2>
<p>It works on my machine, but will it work on Kaggle? Pull the <a href="https://github.com/Kaggle/docker-python">Kaggle Docker Image</a> and setup a test production environment. Use Docker volumes to mount sample test data for inference as well as your &ldquo;Inference&rdquo; package.</p>
<pre><code class="language-julia">docker run --runtime nvidia \
    -v /home/user/CompetitionData:/kaggle/input/testdata \
    -v /home/user/dev/Inference:/kaggle/input/Inference \
    -it gcr.io/kaggle-gpu-images/python /bin/bash</code></pre>
<p>Set up Julia inside the container.</p>
<pre><code class="language-julia"># Download the official binary
wget -nv https://julialang-s3.julialang.org/bin/linux/x64/1.8/julia-1.8.2-linux-x86_64.tar.gz -O /tmp/julia.tar.gz# Unpack it.
tar -x -f /tmp/julia.tar.gz -C /usr/local --strip-components 1</code></pre>
<p>Instantiate the Inference package and then test it.</p>
<pre><code class="language-julia">julia --project&#61;/kaggle/input/Inference -e &quot;using Pkg; Pkg.instantiate&#40;&#41;&quot;
julia --project&#61;/kaggle/input/Inference /kaggle/input/Inference/src/Inference.jl</code></pre>
<p>If the pipeline works, pack ~/.julia into a tarball and move it to one of the shared Docker volumes in order to upload it to Kaggle later.</p>
<pre><code class="language-julia">cd /root
tar -czvf dotjulia.tar.gz .julia
mv dotjulia.tar.gz /kaggle/input/testdata/dotjulia.tar.gz</code></pre>
<p>Exit the production environment.</p>
<h2 id="kaggle_private_datasets">Kaggle Private Datasets</h2>
<p>Prepare to upload the following:</p>
<ol>
<li><p>dotjulia.tar.gz</p>
</li>
<li><p>Inference package</p>
</li>
<li><p>Julia binary</p>
</li>
<li><p>Your model</p>
</li>
</ol>
<p>All except the model must be uploaded as tarballs without the .tar extension. Kaggle recognizes .tar extension and automatically unpacks them in the Dataset container as read only, meaning nothing is executable.</p>
<pre><code class="language-julia"># The Inference package for submission
tar -czvf Inference.tar.gz Inference
mv Inference.tar.gz inferenceapp# The Julia binary.
wget -nv https://julialang-s3.julialang.org/bin/linux/x64/1.8/julia-1.8.2-linux-x86_64.tar.gz -O julia.tar.gz
mv julia.tar.gz julia# ~/.julia from the Docker container is packaged already
mv dotjulia.tar.gz dotjulia# The saved model can be uploaded as .bson or .jld2 directly.</code></pre>
<p>Now we can create <a href="https://www.kaggle.com/datasets">private Kaggle Datasets</a> to upload the following. I&#39;ll name it &#39;packedjulia&#39; &#40;model.jld2 will go into a separate dataset container &#39;models&#39;&#41;.</p>
<ol>
<li><p>dotjulia</p>
</li>
<li><p>inferenceapp</p>
</li>
<li><p>julia</p>
</li>
<li><p>model.jld2</p>
</li>
</ol>
<h2 id="run_the_notebook">Run the Notebook</h2>
<p>Create a new competition Notebook and add as inputs your private Dataset.</p>
<pre><code class="language-julia"># Unpack the Julia binary
&#33;tar -x -f /kaggle/input/packedjulia/julia -C /usr/local --strip-components 1# Unpack the dot julia directory
&#33;tar -x -f /kaggle/input/packedjulia/dotjulia -C /root# Unpack the inference package pipeline
&#33;tar -x -f /kaggle/input/packedjulia/inference -C /tmp# Run the inference on the test data
&#33;julia -t auto --project&#61;/tmp/Inference /tmp/Inference/src/Inference.jl --model /kaggle/input/models/model.jld2</code></pre>
<p>I run the Notebook with internet access first to see if it tries to download any Artifacts. If it does I add them as dependencies to my Inference.jl package and go back to the Docker step to repackage and reupload everything. If it works, turn off internet access and save the Notebook version. Then submit&#33;</p>
<h2 id="final_thoughts">Final thoughts</h2>
<p>See my <a href="https://www.kaggle.com/code/justinochalek/baseline-fastai-julia">submission</a> and <a href="https://github.com/jochalek/HuBMAP">competition repo</a> to see how I did it. Generally I followed the above. This was my first Kaggle submission. Although it ended with me at the back of the pack I learned a lot and I cannot wait to start again.</p>
<h2 id="tldr">TLDR;</h2>
<ol>
<li><p>Pack your source code into a tarball.</p>
</li>
<li><p>Download the Julia binary tarball.</p>
</li>
<li><p>Test it in a &ldquo;production&rdquo; environment with docker.</p>
<pre><code> docker run --runtime nvidia -it gcr.io/kaggle-gpu-images/python /bin/bash</code></pre>
</li>
<li><p>Pack the &ldquo;production&rdquo; environment &rsquo;~/.julia&rsquo; directory into another tarball.</p>
</li>
<li><p>Rename your tarballs from &rsquo;filename.tar.gz&rsquo; to &rsquo;filename&rsquo; and upload them as a private Kaggle Dataset. If the tar file extension is not removed, Kaggle will automatically unpack them in Dataset containers that are not executable.</p>
</li>
<li><p>Create a new Kaggle notebook with your private datasets and unpack the tarballs.</p>
</li>
<li><p>Run your Julia source with Jupyter magic.</p>
<pre><code> &#33;julia inference.jl</code></pre>
</li>
</ol>
 ]]>
  </content:encoded>
    
  <pubDate>Fri, 11 Aug 2023 00:00:00 +0000</pubDate>  
  
  
  <atom:author>
    <atom:name>Jusin Ochalek</atom:name>
  </atom:author>
        
</item>
</channel></rss>